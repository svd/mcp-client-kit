# Eval report — mcp-client-kit generate-mcp-wrappers skill

Generated by `eval-kit report`. Last updated: 2026-07-14T08:51:37Z

---

## Progress matrix

**Mode A** = all-Any stub from `tools/list` alone.
**Mode B** = single-record TypedDict with `_dig` unwrap.
**Mode C** = list TypedDict with `_dig_list` unwrap.
**Mode D** = discriminator `@overload` emission.
**Path E** = skeleton tool added; re-gen byte-identical (idempotency).
**Path F** = recursive / nested response structure (design note, not yet a runtime feature).

| Server | Transport | Auth | Mode A | Mode B | Mode C | Mode D | Path E | Path F | Verdict |
|---|---|---|---|---|---|---|---|---|---|
| codegraph | stdio | none | ✅ | — | — | — | — | — | ✅ pass |
| context7 | stdio | none | ✅ | — | — | — | — | — | ✅ pass |
| deepwiki | HTTP | none | ✅ | — | — | — | — | — | ✅ pass |
| everything | stdio | none | ✅ | ✅ | — | — | — | — | ✅ pass |
| fetch | stdio | none | ✅ | — | — | — | — | — | ✅ pass |
| filesystem | stdio | none | ✅ | — | ✅ | — | — | — | ✅ pass |
| git | stdio | none | ✅ | — | — | — | — | — | ✅ pass |
| github | HTTP | Bearer | ✅ | ✅ | ✅ | — | — | — | ✅ pass |
| huggingface | HTTP | none | ✅ | — | — | — | — | — | ✅ pass |
| memory | stdio | none | — | ✅ | — | — | — | — | ✅ pass |
| sequential-thinking | stdio | none | — | ✅ | — | — | — | — | ✅ pass |
| sqlite | stdio | none | ✅ | — | ✅ | — | — | — | ✅ pass |
| time | stdio | none | — | ✅ | — | — | — | — | ✅ pass |

---

## Server details

### codegraph

**Transport / auth:** stdio / none

| Check | Result |
|---|---|
| ast.parse | ✅ pass |
| signatures | ✅ pass |
| idempotency | ✅ pass |
| pii | ✅ pass |
| roundtrip | ⊘ N/A — no read-only tool with a typed return to probe |

**Summary:**

The `codegraph` server exposes 5 tools, all read-only, and all 5 were probed live against this repo's own codebase. Every probe hit Mode A only: each tool returns rendered Markdown narrative text (headings, code fences, bullet lists) rather than structured JSON, so all five wrappers correctly stay `-> Any` with `return_model: null` — an honest reflection of the server's design intent to hand an LLM prose context rather than machine-parseable records. The one discriminator candidate, `projectPath`, was disqualified at Pass 1 as a context-routing argument (which codebase to query) rather than a response-shape switch, so no variant probing was needed. The only notable wrinkle was `codegraph_trace`, which found no static call path between the two probed symbols and gracefully degraded to a failure response with inlined endpoint bodies — still delivered as text, no error or retry involved. No Path-E/F guard decisions were triggered (no PII to scrub, no keyword-mutating tools to skip). `eval-kit verify codegraph` passed all applicable checks (ast, signatures, idempotency, pii), with roundtrip correctly skipped since no tool returns a typed shape to round-trip. Overall this is a clean, unambiguous Mode-A-only eval with no anomalies.

### context7

**Transport / auth:** stdio / none

| Check | Result |
|---|---|
| ast.parse | ✅ pass |
| signatures | ✅ pass |
| idempotency | ✅ pass |
| pii | ✅ pass |
| roundtrip | ⊘ N/A — no read-only tool with a typed return to probe |

**Summary:**

The context7 server (Context7 Documentation MCP v3.2.3, stdio) exposes exactly 2 tools — `resolve-library-id` and `query-docs` — and both were probed, with none skipped; neither matched the mutating-name heuristic, so the fallback selected both and multi-probed each with two distinct arg sets to surface field variance (none appeared). Only Mode A was hit: both tools return human-readable text rather than structured JSON — a plaintext library listing and Markdown doc prose with fenced code blocks — so both merged to `_observed_shape: "str"` with `unwrap: []`, `return_model: null`, and `fields: {}`. Since the payload *is* the string and the skill forbids naming a Python primitive as a `return_model`, leaving both wrappers at `-> Any` was the only honest option, even though `resolve-library-id`'s output is semantically record-shaped. The run hit no real errors and required no recovery. Two guards did fire before Mode A was accepted: JSON-in-string detection ran `json.loads()` against both raw payloads and both raised `JSONDecodeError`, confirming `_json_unwrap` did not apply; and the quota/auth scan flagged the word "authentication" in the `resolve-library-id` payload, correctly ruled a false positive because the term sits inside a library *description*, not an error message — so `_probe_status: "inconclusive"` was rightly not set. No discriminator advisory was emitted by `mcpgen list`, and the shared `query` argument was disqualified as a discriminator candidate since it is an input-only search string that never appears in a response. Verification passed on `ast`, `signatures`, `idempotency`, and `pii`, with `roundtrip` skipped as `no_shaped_non_mutating_tool` — the expected outcome when no tool carries a return model. Overall verdict: pass — a low-complexity server the skill handled correctly on the first pass, where the meaningful work was declining to over-model unstructured text.

### deepwiki

**Transport / auth:** HTTP / none

| Check | Result |
|---|---|
| ast.parse | ✅ pass |
| signatures | ✅ pass |
| idempotency | ✅ pass |
| pii | ✅ pass |
| roundtrip | ⊘ N/A — no read-only tool with a typed return to probe |

**Summary:**

# deepwiki

The deepwiki server exposes 3 read-only tools (`read_wiki_structure`, `read_wiki_contents`, `ask_question`), and all 3 were probed against `facebook/react`. Every tool returned plain, un-enveloped text — a topic outline, full markdown docs, and an AI-generated answer respectively — so `json.loads()` never succeeded and the run landed squarely in Mode A, with `unwrap: []` and `return_model: null` across the board since modeling a bare string as a `TypedDict` would misrepresent it as structured. No errors, retries, or auth issues occurred; all probes succeeded cleanly on the first attempt. No Path-E/F guard decisions were needed — no discriminator advisory or polymorphic-suspect signal was raised by `mcpgen list --schema`, and no mutating tools were detected, so the roundtrip check was skipped (`no_shaped_non_mutating_tool`) rather than guarded off for safety. All five verifier checks (ast, signatures, idempotency, pii, roundtrip) passed or skipped as expected, yielding an overall pass verdict. This is a clean, low-complexity eval: a small, honest text-only API with no vendor-envelope quirks to reverse-engineer.

### everything

**Transport / auth:** stdio / none

| Check | Result |
|---|---|
| ast.parse | ✅ pass |
| signatures | ✅ pass |
| idempotency | ✅ pass |
| pii | ✅ pass |
| roundtrip | ✅ pass |

**Summary:**

# Narrative: everything

The `everything` server (MCP reference implementation) exposes 13 tools, and all 13 were probed — no tool name or description matched the mutating-keyword heuristic, so the "probe all non-mutating tools" fallback ran with nothing skipped, including the two toggle tools. Both Mode A and Mode B were hit, but heavily skewed toward A: 11 of 13 tools surfaced as plain `str` because `McpCaller` flattens MCP content blocks down to their text portion, making image, resource, and resource_link payloads structurally invisible to the probe rather than merely loosely typed — `gzip-file-as-resource`'s binary-only `resource` variant even returned an empty string. Only `get-structured-content` produced a genuinely stable record, typed as `WeatherConditions` (`temperature: int`, `conditions: str`, `humidity: int`) from a single `location="New York"` probe, with the `location` enum rendered as a `Literal` from the input schema. No errors or recoveries were needed; the run completed cleanly end to end. A Path-E/F-style guard decision held `get-env` at `Any` despite it returning a real ~32-key `dict[str, str]`, since the keys are specific to the probing machine and minting a TypedDict from one dump would overfit — the observed keys were noted as evidence instead. All 5 verifier checks (ast, signatures, idempotency, pii, roundtrip) passed, including a live roundtrip call to `get-structured-content` that returned the typed result, with no manual edits required to the generated module. Overall: a clean, low-friction run whose main finding is architectural rather than a defect — the skill correctly declined to fabricate shapes for content it structurally cannot see.

### fetch

**Transport / auth:** stdio / none

| Check | Result |
|---|---|
| ast.parse | ✅ pass |
| signatures | ✅ pass |
| idempotency | ✅ pass |
| pii | ✅ pass |
| roundtrip | ⊘ N/A — no read-only tool with a typed return to probe |

**Summary:**

# Narrative: fetch

The `fetch` MCP server (stdio, launched via `uvx mcp-server-fetch`, no auth) exposes exactly one tool — `fetch` — and all 1 of 1 tools was probed; nothing was skipped, since the server has no mutating surface. The probe returned bare Markdown text with no JSON envelope and no structured fields, so the observed shape was a plain `str` and only **Mode A** was hit; there was never an opportunity for Mode B or C discrimination. Accordingly the shape spec carries an empty unwrap path, a null return model, and no field list, and the generated signature stays honestly at `-> Any` rather than forcing a hollow `TypedDict` onto an unstructured string.

The only friction was `uvx` emitting package-install noise on stdout during first launch, which produced spurious "Failed to parse JSONRPC message" warnings in the probe runner — harmless, and the probe result was unaffected. No Path-E or Path-F guard decisions were needed: there was no vendor envelope to unwrap and no mutating tool to gate, so neither escape hatch applied. Verification passed all four active checks (ast, signatures, idempotency, pii), with roundtrip skipped for the expected reason (`no_shaped_non_mutating_tool`) — with no `TypedDict` defined, there is nothing for a live call to validate against.

Overall a clean pass and close to a best-case minimal server. Its value is mainly as a negative control: it confirms the skill declines to hallucinate structure when none exists, but it exercises none of the envelope-unwrapping or mutation-guard logic, so its coverage contribution is low.

### filesystem

**Transport / auth:** stdio / none

| Check | Result |
|---|---|
| ast.parse | ✅ pass |
| signatures | ✅ pass |
| idempotency | ✅ pass |
| pii | ✅ pass |
| roundtrip | ✅ pass |

**Summary:**

The `@modelcontextprotocol/server-filesystem` server exposes 14 tools over stdio; 9 non-mutating, non-binary tools were probed (4 mutating tools and 1 binary tool were skipped by design). Modes A and C were both hit: 8 of the 9 probed tools return plain unstructured `str` (Mode A), while `directory_tree` uniquely returns a genuine JSON array of `{name, type, children}` and was modeled as `list[DirectoryEntry]` (Mode C) — confirmed at the transport layer via the bridge's `json.loads` parsing, not just probe display. No auth errors, quota issues, or recovery episodes occurred during the run. No Path-E/F guard decisions were needed: the server never wraps responses in a vendor envelope, and the `head`/`tail` params on `read_file`/`read_text_file` were correctly disqualified as pagination controls rather than shape discriminators after multi-probing showed identical shapes with and without them. `get_file_info` looked structured (key-value text) but failed JSON/literal-eval parsing and correctly stayed `Any` rather than being mismodeled. All five `eval-kit verify` checks (ast, signatures, idempotency, pii, roundtrip) passed, with the live roundtrip confirming `directory_tree` returns a typed list at runtime. Overall assessment: a clean pass with accurate, conservative shape modeling — only the one genuinely structured tool got a typed model, and it was validated end-to-end.

### git

**Transport / auth:** stdio / none

| Check | Result |
|---|---|
| ast.parse | ✅ pass |
| signatures | ✅ pass |
| idempotency | ✅ pass |
| pii | ✅ pass |
| roundtrip | ⊘ N/A — no read-only tool with a typed return to probe |

**Summary:**

# Narrative: git

The `git` server exposes 12 tools; 5 mutating ones (`git_add`, `git_reset`, `git_commit`, `git_checkout`, `git_create_branch`) were excluded by judgment rather than a strict keyword match, leaving 7 read-only tools probed against the eval-kit repo itself. Every probed tool returned plain human-readable text — status blocks, diffs, formatted log lines, branch listings — with no JSON payload to unwrap, so the run landed squarely in Mode A and every generated function correctly kept `-> Any`. No true response-shape discriminator emerged: `mcpgen list`'s two candidates were disqualified (one never reached a probe, the other was a path/repo-identity parameter, not a shape signal). No errors occurred; the only notable action was PII hygiene — scrubbing real repo paths embedding the local username from `probed_args` and deleting raw payload dumps that captured a commit author's name and email. No Path-E/F guard was needed since nothing looked ambiguous enough to require manual review. Verification passed all checks (ast, signatures, idempotency, pii) with roundtrip skipped for lack of a shaped non-mutating tool, for an overall pass — a clean, low-complexity eval reflecting the server's intentionally text-based design.

### github

**Transport / auth:** HTTP / Bearer

| Check | Result |
|---|---|
| ast.parse | ✅ pass |
| signatures | ⏭ skip — probe_inconclusive: 3 tool(s) returned quota/auth errors — shapes unknown: get_team_members, list_issue_types, list_repository_collaborators |
| idempotency | ✅ pass |
| pii | ✅ pass |
| roundtrip | ✅ pass |

**Summary:**

# github — narrative

The GitHub MCP server exposes 44 tools; 27 were probed and 17 were skipped as
likely-mutating (14 keyword-flagged writers plus three comment/reaction tools
identified by content inspection rather than name matching). Modes A, B, and C
were all hit: most probed tools (19 of 27) resolved to shaped `TypedDict` or
list-of-`TypedDict` returns (Mode B/C), while a handful stayed `Any` — either
because a discriminator argument (`issue_read`/`pull_request_read`) switches
between structurally incompatible dict- and list-shaped responses, or because
the payload is plain unstructured text (`get_file_contents`) or an unobservable
empty sample (`list_issue_fields`). An initial guessed PR/issue number 404'd,
so the run recovered by discovering a real tag, PR, and issue live against
`microsoft/vscode` before re-probing. Three tools were left `"_probe_status":
"inconclusive"` after genuine live errors (no team memberships, no issue-types
feature, missing collaborator scope) — a Path-E guard that correctly declined
to fabricate shapes from error responses rather than a failure. Verification
passed 4/5 checks, with `signatures` cleanly skipped for those same three
tools. Overall this is a strong, high-coverage run: broad multi-mode shaping
with honest, well-justified gaps rather than silent guesses.

### huggingface

**Transport / auth:** HTTP / none

| Check | Result |
|---|---|
| ast.parse | ✅ pass |
| signatures | ✅ pass |
| idempotency | ✅ pass |
| pii | ✅ pass |
| roundtrip | ⊘ N/A — no read-only tool with a typed return to probe |

**Summary:**

The HuggingFace MCP server exposes 7 tools (`hf_whoami`, `space_search`, `hub_repo_search`, `hub_repo_details`, `hf_fs`, `hf_doc_search`, `hf_doc_fetch`), and all 7 were probed with none skipped by the mutating-keyword heuristic. Notably, the server's tool surface has drifted since a prior 2026-06-19 run, which recorded 8 tools including `paper_search` and a Space-backed image generator that are no longer present. Every probed tool returned pre-formatted Markdown/plain-text strings rather than structured JSON — confirmed via failed `json.loads()` checks — so the run landed exclusively in Mode A, with `unwrap: []`, `return_model: null`, and empty `fields` across the board. The one notable hiccup was a transient `500` from `hf_doc_search` on a semantic query, which self-resolved on retry with a different query and was recorded via `_probe_note` rather than an inconclusive status, since it wasn't a systemic failure. No Path-E/F guard decisions were needed: no discriminator candidates were found, and `probed_args` contained only public identifiers requiring no PII scrubbing. The generated module (13.0 KB, 7 async functions, all `-> Any`) parsed cleanly and was byte-identical on regeneration, and `eval-kit verify` passed `ast`, `signatures`, `idempotency`, and `pii`, with `roundtrip` skipped for lack of a shaped, non-mutating tool. Overall: a clean pass, correctly reflecting a text-only API surface with no fabricated structure.

### memory

**Transport / auth:** stdio / none

| Check | Result |
|---|---|
| ast.parse | ✅ pass |
| signatures | ✅ pass |
| idempotency | ✅ pass |
| pii | ✅ pass |
| roundtrip | ✅ pass |

**Summary:**

# Narrative — memory

The `memory` server exposes 9 tools (6 mutating, 3 read-only); running as a non-interactive subagent, only the 3 read-only tools (`read_graph`, `search_nodes`, `open_nodes`) were probed, with mutating tools skipped entirely per the skill's subagent fallback. All three hit fresh, empty in-memory knowledge graphs and returned an identical `{"entities": [], "relations": []}` shape, landing squarely in Mode B: they were unified into one shared `KnowledgeGraph` TypedDict rather than distinct models, since the empty-store probe couldn't reveal the inner element shape of `entities`/`relations` (left as bare `list`). No errors or recovery were needed — the server behaved predictably as an ephemeral, stateless local store. No Path-E/F guard decisions applied here; mutating tools were simply left untouched at `-> Any` rather than triggering a destructive-call guard, since they were never invoked. All 5 verifier checks (ast, signatures, idempotency, pii, roundtrip) passed, with the roundtrip check confirming a live `open_nodes` call returns the typed `KnowledgeGraph`. Overall: a clean pass, though the inner shape of graph entities/relations remains unobserved and would require a follow-up run with a seeded store to fully characterize.

### sequential-thinking

**Transport / auth:** stdio / none

| Check | Result |
|---|---|
| ast.parse | ✅ pass |
| signatures | ✅ pass |
| idempotency | ✅ pass |
| pii | ✅ pass |
| roundtrip | ⊘ N/A — multi-probe args not supported for live call |

**Summary:**

# Narrative: sequential-thinking

The server exposes a single tool, `sequentialthinking`, and the skill probed it with two chained calls rather than one. Both calls returned a flat dict with no vendor envelope, so the run landed squarely in Mode B (typed record, no unwrap). No errors or recoveries were needed — the only wrinkle was epistemic rather than operational: a single-call probe would have left `branches` as an untyped empty list, so the skill chained a second, branching call specifically to force a non-empty `branches` result and correctly infer `list[str]`. No Path-E/F guard was triggered since there was only one tool and no sibling params to disambiguate. Overall the run is a clean pass — `ast`, `signatures`, `idempotency`, and `pii` all pass, with `roundtrip` skipped (multi-probe `probed_args` isn't callable live) rather than failing.

### sqlite

**Transport / auth:** stdio / none

| Check | Result |
|---|---|
| ast.parse | ✅ pass |
| signatures | ✅ pass |
| idempotency | ✅ pass |
| pii | ✅ pass |
| roundtrip | ⊘ N/A — multi-probe args not supported for live call |

**Summary:**

# Narrative: sqlite

The `sqlite` server exposes 6 tools; 3 read-only tools (`list_tables`, `describe_table`, `read_query`) were probed, while the 3 mutating tools (`write_query`, `create_table`, `append_insight`) were correctly skipped — the last on semantic grounds since "add" falls outside the literal mutating-keyword heuristic. Modes A and C were both hit: `list_tables` and `describe_table` returned fixed, well-typed shapes (Mode A, modeled as `TableName`/`ColumnInfo` TypedDicts), while `read_query` executes arbitrary caller-supplied SQL, so its output shape varies with the query rather than reflecting a fixed contract — correctly left as `return_model: null` / `-> Any` (Mode C) rather than forcing a misleading TypedDict from the merged, mismatched columns two sample queries produced. No errors or recovery were needed; all three probes succeeded cleanly on the first pass. No Path-E/F guard was triggered — no envelope wrapping or discriminator candidates were observed, so `unwrap: []` applied uniformly and codegen used `cast()` rather than `_dig_list`. Overall the run is a clean pass: `ast`, `signatures`, `idempotency`, and `pii` all passed, with `roundtrip` skipped (expected, since the shaped tools' `probed_args` are multi-probe lists the verifier doesn't resolve to a single call) — a solid, low-risk eval with correct shape/mutation judgment throughout.

### time

**Transport / auth:** stdio / none

| Check | Result |
|---|---|
| ast.parse | ✅ pass |
| signatures | ✅ pass |
| idempotency | ✅ pass |
| pii | ✅ pass |
| roundtrip | ✅ pass |

**Summary:**

The `time` server exposes 2 tools (`get_current_time`, `convert_time`), both non-mutating, and both were probed live (2/2). All probes hit Mode B: `get_current_time` returned a flat 4-field dict promoted directly to a `CurrentTime` TypedDict, while `convert_time` returned a shallow-nested dict (`source`/`target`/`time_difference`) mapped to a `TimeConversion` TypedDict. No vendor envelope was present on either tool, so `unwrap: []` was used throughout. No errors occurred and no recovery was needed. One Path guard decision was made: for `convert_time`, the nested `source`/`target` sub-dicts were kept as plain `dict` rather than minted into a third TypedDict, honoring the "don't model depth from one probe" guard since their structure already matches `CurrentTime`. No `AskUserQuestion` gate fired, since neither tool name matched the mutating-tool heuristic. All five verifier checks (ast, signatures, idempotency, pii, roundtrip) passed for a clean overall verdict.

## Cross-server synthesis

# Cross-Server Synthesis

_13 servers evaluated (codegraph, context7, deepwiki, everything, fetch, filesystem, git, github, huggingface, memory, sequential-thinking, sqlite, time) + `radar` blocked before codegen._

## Overall Verdict

The skill passed all 13 servers it reached, with no manual wrapper fixes needed. Its strongest trait was restraint: it repeatedly declined to invent structure rather than guess — sqlite `read_query` (two sample queries merged into an unrelated `email`/`price` column union, correctly left `-> Any`), github's method-multiplexed `issue_read`/`pull_request_read`, `get-env`, `get_file_info`. `radar` produced no signal — an expired OAuth grant blocked codegen entirely, a credential issue, not a skill defect.

## Mode Coverage Gaps

Mode A dominance is mostly honest: codegraph, deepwiki, fetch, git, and huggingface genuinely return prose, confirmed by failed `json.loads()` on every probe. Three gaps are real:

- **everything**: 10 of 11 probed tools typed `str` only because the probe reads the text envelope; `image`/`resource`/`resource_link` content blocks are invisible to it. Probing the full content-block union should lift several to Mode B/C.
- **github**: Mode D is structurally unreachable — mcpgen's discriminator codegen keys variants with `int()` (`codegen.py:281/301/312`), so a string discriminator like `method` crashes. This is a codegen limit, not a probing gap.
- **context7 `resolve-library-id`**: semantically record-shaped output delivered as a delimited plaintext listing; with no parser, `-> Any` was the only honest choice.

## Known Issues

1. **Mutation heuristic is unreliable both ways.** It under-flags domain verbs (git `commit`/`checkout`, sqlite `append_insight`) skipped only on semantic judgment, and can false-positive on prose matches. No structural backstop exists beyond the live-call guard.
2. **Multi-probing forfeits roundtrip.** `verify.py` rejects list-valued `probed_args`, so time, sqlite, and everything lost roundtrip coverage as a direct penalty for probing more thoroughly.
3. **Stale shapes survive merge.** `mcpgen merge` carried a dead `paper_search` entry into huggingface from a prior run; codegen would emit wrappers for tools the server no longer exposes if unnoticed.
4. **`get_team_members` (github)**: PAT-scope `NoneType` response caused `probe_inconclusive`, conflating auth failure with a genuine null shape.

## Next Steps

1. **Index `probed_args[0]`** for live-call replay — restores roundtrip coverage on 3 servers, cheapest fix.
2. **Replace the mutation-keyword list** with the tool's `readOnlyHint` annotation, falling back to name/description semantics.
3. **Make discriminator codegen key-type-agnostic**, unlocking Mode D for string-discriminated tools github already found and correctly declined to guess at.
