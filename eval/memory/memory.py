"""Generated MCP wrappers for the 'memory' server. DO NOT hand-edit signatures.

Generated by mcpgen from a live tools/list. Each function forwards to an
injected McpCaller (see mcpgen.seam); the auth/transport backend is the
caller's concern, not this module's.
"""
from __future__ import annotations

from typing import Any, TypedDict, cast

from mcpgen.seam import McpCaller

SERVER = 'memory'



class KnowledgeGraph(TypedDict, total=False):
    entities: list
    relations: list


async def add_observations(caller: McpCaller, *, observations: list[dict]) -> Any:
    """Add new observations to existing entities in the knowledge graph

    Args:
        observations:
    """
    args: dict[str, Any] = {"observations": observations}
    return await caller.call(SERVER, "add_observations", args)

add_observations.__schema__ = {'$schema': 'http://json-schema.org/draft-07/schema#', 'type': 'object', 'properties': {'observations': {'type': 'array', 'items': {'type': 'object', 'properties': {'entityName': {'type': 'string', 'description': 'The name of the entity to add the observations to'}, 'contents': {'type': 'array', 'items': {'type': 'string'}, 'description': 'An array of observation contents to add'}}, 'required': ['entityName', 'contents']}}}, 'required': ['observations']}


async def create_entities(caller: McpCaller, *, entities: list[dict]) -> Any:
    """Create multiple new entities in the knowledge graph

    Args:
        entities:
    """
    args: dict[str, Any] = {"entities": entities}
    return await caller.call(SERVER, "create_entities", args)

create_entities.__schema__ = {'$schema': 'http://json-schema.org/draft-07/schema#', 'type': 'object', 'properties': {'entities': {'type': 'array', 'items': {'type': 'object', 'properties': {'name': {'type': 'string', 'description': 'The name of the entity'}, 'entityType': {'type': 'string', 'description': 'The type of the entity'}, 'observations': {'type': 'array', 'items': {'type': 'string'}, 'description': 'An array of observation contents associated with the entity'}}, 'required': ['name', 'entityType', 'observations']}}}, 'required': ['entities']}


async def create_relations(caller: McpCaller, *, relations: list[dict]) -> Any:
    """Create multiple new relations between entities in the knowledge graph. Relations should be in active voice

    Args:
        relations:
    """
    args: dict[str, Any] = {"relations": relations}
    return await caller.call(SERVER, "create_relations", args)

create_relations.__schema__ = {'$schema': 'http://json-schema.org/draft-07/schema#', 'type': 'object', 'properties': {'relations': {'type': 'array', 'items': {'type': 'object', 'properties': {'from': {'type': 'string', 'description': 'The name of the entity where the relation starts'}, 'to': {'type': 'string', 'description': 'The name of the entity where the relation ends'}, 'relationType': {'type': 'string', 'description': 'The type of the relation'}}, 'required': ['from', 'to', 'relationType']}}}, 'required': ['relations']}


async def delete_entities(caller: McpCaller, *, entityNames: list[str]) -> Any:
    """Delete multiple entities and their associated relations from the knowledge graph

    Args:
        entityNames: An array of entity names to delete
    """
    args: dict[str, Any] = {"entityNames": entityNames}
    return await caller.call(SERVER, "delete_entities", args)

delete_entities.__schema__ = {'$schema': 'http://json-schema.org/draft-07/schema#', 'type': 'object', 'properties': {'entityNames': {'type': 'array', 'items': {'type': 'string'}, 'description': 'An array of entity names to delete'}}, 'required': ['entityNames']}


async def delete_observations(caller: McpCaller, *, deletions: list[dict]) -> Any:
    """Delete specific observations from entities in the knowledge graph

    Args:
        deletions:
    """
    args: dict[str, Any] = {"deletions": deletions}
    return await caller.call(SERVER, "delete_observations", args)

delete_observations.__schema__ = {'$schema': 'http://json-schema.org/draft-07/schema#', 'type': 'object', 'properties': {'deletions': {'type': 'array', 'items': {'type': 'object', 'properties': {'entityName': {'type': 'string', 'description': 'The name of the entity containing the observations'}, 'observations': {'type': 'array', 'items': {'type': 'string'}, 'description': 'An array of observations to delete'}}, 'required': ['entityName', 'observations']}}}, 'required': ['deletions']}


async def delete_relations(caller: McpCaller, *, relations: list[dict]) -> Any:
    """Delete multiple relations from the knowledge graph

    Args:
        relations: An array of relations to delete
    """
    args: dict[str, Any] = {"relations": relations}
    return await caller.call(SERVER, "delete_relations", args)

delete_relations.__schema__ = {'$schema': 'http://json-schema.org/draft-07/schema#', 'type': 'object', 'properties': {'relations': {'type': 'array', 'items': {'type': 'object', 'properties': {'from': {'type': 'string', 'description': 'The name of the entity where the relation starts'}, 'to': {'type': 'string', 'description': 'The name of the entity where the relation ends'}, 'relationType': {'type': 'string', 'description': 'The type of the relation'}}, 'required': ['from', 'to', 'relationType']}, 'description': 'An array of relations to delete'}}, 'required': ['relations']}


async def open_nodes(caller: McpCaller, *, names: list[str]) -> KnowledgeGraph:
    """Open specific nodes in the knowledge graph by their names

    Args:
        names: An array of entity names to retrieve
    """
    args: dict[str, Any] = {"names": names}
    return cast("KnowledgeGraph", await caller.call(SERVER, "open_nodes", args))

open_nodes.__schema__ = {'$schema': 'http://json-schema.org/draft-07/schema#', 'type': 'object', 'properties': {'names': {'type': 'array', 'items': {'type': 'string'}, 'description': 'An array of entity names to retrieve'}}, 'required': ['names']}


async def read_graph(caller: McpCaller) -> KnowledgeGraph:
    """Read the entire knowledge graph"""
    return cast("KnowledgeGraph", await caller.call(SERVER, "read_graph", {}))

read_graph.__schema__ = {'$schema': 'http://json-schema.org/draft-07/schema#', 'type': 'object', 'properties': {}}


async def search_nodes(caller: McpCaller, *, query: str) -> KnowledgeGraph:
    """Search for nodes in the knowledge graph based on a query

    Args:
        query: The search query to match against entity names, types, and observation content
    """
    args: dict[str, Any] = {"query": query}
    return cast("KnowledgeGraph", await caller.call(SERVER, "search_nodes", args))

search_nodes.__schema__ = {'$schema': 'http://json-schema.org/draft-07/schema#', 'type': 'object', 'properties': {'query': {'type': 'string', 'description': 'The search query to match against entity names, types, and observation content'}}, 'required': ['query']}
