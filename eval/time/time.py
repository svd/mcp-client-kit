"""Generated MCP wrappers for the 'time' server. DO NOT hand-edit signatures.

Generated by mcpgen from a live tools/list. Each function forwards to an
injected McpCaller (see mcpgen.seam); the auth/transport backend is the
caller's concern, not this module's.
"""
from __future__ import annotations

from typing import Any, TypedDict, cast

from mcpgen.seam import McpCaller

SERVER = 'time'



class TimeConversion(TypedDict, total=False):
    source: dict
    target: dict
    time_difference: str


class CurrentTime(TypedDict, total=False):
    timezone: str
    datetime: str
    day_of_week: str
    is_dst: bool


async def convert_time(caller: McpCaller, *, source_timezone: str, time: str, target_timezone: str) -> TimeConversion:
    """Convert time between timezones

    Args:
        source_timezone: Source IANA timezone name (e.g., 'America/New_York', 'Europe/London'). Use 'Europe/Minsk' as local timezone if no source timezone provided by the user.
        time: Time to convert in 24-hour format (HH:MM)
        target_timezone: Target IANA timezone name (e.g., 'Asia/Tokyo', 'America/San_Francisco'). Use 'Europe/Minsk' as local timezone if no target timezone provided by the user.
    """
    args: dict[str, Any] = {"source_timezone": source_timezone, "time": time, "target_timezone": target_timezone}
    return cast("TimeConversion", await caller.call(SERVER, "convert_time", args))

convert_time.__schema__ = {'type': 'object', 'properties': {'source_timezone': {'type': 'string', 'description': "Source IANA timezone name (e.g., 'America/New_York', 'Europe/London'). Use 'Europe/Minsk' as local timezone if no source timezone provided by the user."}, 'time': {'type': 'string', 'description': 'Time to convert in 24-hour format (HH:MM)'}, 'target_timezone': {'type': 'string', 'description': "Target IANA timezone name (e.g., 'Asia/Tokyo', 'America/San_Francisco'). Use 'Europe/Minsk' as local timezone if no target timezone provided by the user."}}, 'required': ['source_timezone', 'time', 'target_timezone']}


async def get_current_time(caller: McpCaller, *, timezone: str) -> CurrentTime:
    """Get current time in a specific timezone

    Args:
        timezone: IANA timezone name (e.g., 'America/New_York', 'Europe/London'). Use 'Europe/Minsk' as local timezone if no timezone provided by the user.
    """
    args: dict[str, Any] = {"timezone": timezone}
    return cast("CurrentTime", await caller.call(SERVER, "get_current_time", args))

get_current_time.__schema__ = {'type': 'object', 'properties': {'timezone': {'type': 'string', 'description': "IANA timezone name (e.g., 'America/New_York', 'Europe/London'). Use 'Europe/Minsk' as local timezone if no timezone provided by the user."}}, 'required': ['timezone']}
