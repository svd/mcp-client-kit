"""Generated MCP wrappers for the 'codegraph' server. DO NOT hand-edit signatures.

Generated by mcpgen from a live tools/list. Each function forwards to an
injected McpCaller (see mcpgen.seam); the auth/transport backend is the
caller's concern, not this module's.
"""
from __future__ import annotations

from typing import Any, Literal

from mcpgen.seam import McpCaller

SERVER = 'codegraph'



async def codegraph_context(caller: McpCaller, *, task: str, maxNodes: float | None = None, includeCode: bool | None = None, projectPath: str | None = None) -> Any:
    """PRIMARY TOOL — call FIRST for any "how does X work"/architecture/bug question. Returns entry points + related symbols + key code in one call; usually answers without further search/Read/Grep. Provides CODE context, not product requirements.

    Args:
        task: Description of the task, bug, or feature to build context for
        maxNodes: Maximum symbols to include (default: 20). Default: 20
        includeCode: Include code snippets for key symbols (default: true). Default: True
        projectPath: Path to a different project with .codegraph/ initialized. If omitted, uses current project. Use this to query other codebases.
    """
    args: dict[str, Any] = {"task": task}
    if maxNodes is not None:
        args["maxNodes"] = maxNodes
    if includeCode is not None:
        args["includeCode"] = includeCode
    if projectPath is not None:
        args["projectPath"] = projectPath
    return await caller.call(SERVER, "codegraph_context", args)

codegraph_context.__schema__ = {'type': 'object', 'properties': {'task': {'type': 'string', 'description': 'Description of the task, bug, or feature to build context for'}, 'maxNodes': {'type': 'number', 'description': 'Maximum symbols to include (default: 20)', 'default': 20}, 'includeCode': {'type': 'boolean', 'description': 'Include code snippets for key symbols (default: true)', 'default': True}, 'projectPath': {'type': 'string', 'description': 'Path to a different project with .codegraph/ initialized. If omitted, uses current project. Use this to query other codebases.'}}, 'required': ['task']}


async def codegraph_explore(caller: McpCaller, *, query: str, maxFiles: float | None = None, projectPath: str | None = None) -> Any:
    """Source of SEVERAL related symbols grouped by file, in one capped call. Query is a bag of symbol/file names (not a question). Returned source is verbatim Read-equivalent — do not re-open shown files. Prefer over chained codegraph_node. Budget: make at most 1 calls for this project (5 files indexed).

    Args:
        query: Symbol names, file names, or short code terms to explore (e.g., "AuthService loginUser session-manager", "GraphTraverser BFS impact traversal.ts"). Use codegraph_search first to find relevant names.
        maxFiles: Maximum number of files to include source code from (default: 12). Default: 12
        projectPath: Path to a different project with .codegraph/ initialized. If omitted, uses current project. Use this to query other codebases.
    """
    args: dict[str, Any] = {"query": query}
    if maxFiles is not None:
        args["maxFiles"] = maxFiles
    if projectPath is not None:
        args["projectPath"] = projectPath
    return await caller.call(SERVER, "codegraph_explore", args)

codegraph_explore.__schema__ = {'type': 'object', 'properties': {'query': {'type': 'string', 'description': 'Symbol names, file names, or short code terms to explore (e.g., "AuthService loginUser session-manager", "GraphTraverser BFS impact traversal.ts"). Use codegraph_search first to find relevant names.'}, 'maxFiles': {'type': 'number', 'description': 'Maximum number of files to include source code from (default: 12)', 'default': 12}, 'projectPath': {'type': 'string', 'description': 'Path to a different project with .codegraph/ initialized. If omitted, uses current project. Use this to query other codebases.'}}, 'required': ['query']}


async def codegraph_node(caller: McpCaller, *, symbol: str, includeCode: bool | None = None, projectPath: str | None = None) -> Any:
    """One symbol's location, signature, callers/callees trail. includeCode=true returns the verbatim body. Use codegraph_trace for full paths instead of chaining nodes.

    Args:
        symbol: Name of the symbol to get details for
        includeCode: Include full source code (default: false to minimize context). Default: False
        projectPath: Path to a different project with .codegraph/ initialized. If omitted, uses current project. Use this to query other codebases.
    """
    args: dict[str, Any] = {"symbol": symbol}
    if includeCode is not None:
        args["includeCode"] = includeCode
    if projectPath is not None:
        args["projectPath"] = projectPath
    return await caller.call(SERVER, "codegraph_node", args)

codegraph_node.__schema__ = {'type': 'object', 'properties': {'symbol': {'type': 'string', 'description': 'Name of the symbol to get details for'}, 'includeCode': {'type': 'boolean', 'description': 'Include full source code (default: false to minimize context)', 'default': False}, 'projectPath': {'type': 'string', 'description': 'Path to a different project with .codegraph/ initialized. If omitted, uses current project. Use this to query other codebases.'}}, 'required': ['symbol']}


async def codegraph_search(caller: McpCaller, *, query: str, kind: Literal['function', 'method', 'class', 'interface', 'type', 'variable', 'route', 'component'] | None = None, limit: float | None = None, projectPath: str | None = None) -> Any:
    """Quick symbol search by name. Returns locations only (no code). Use codegraph_context instead for comprehensive task context.

    Args:
        query: Symbol name or partial name (e.g., "auth", "signIn", "UserService")
        kind: Filter by node kind. One of: 'function', 'method', 'class', 'interface', 'type', 'variable', 'route', 'component'
        limit: Maximum results (default: 10). Default: 10
        projectPath: Path to a different project with .codegraph/ initialized. If omitted, uses current project. Use this to query other codebases.
    """
    args: dict[str, Any] = {"query": query}
    if kind is not None:
        args["kind"] = kind
    if limit is not None:
        args["limit"] = limit
    if projectPath is not None:
        args["projectPath"] = projectPath
    return await caller.call(SERVER, "codegraph_search", args)

codegraph_search.__schema__ = {'type': 'object', 'properties': {'query': {'type': 'string', 'description': 'Symbol name or partial name (e.g., "auth", "signIn", "UserService")'}, 'kind': {'type': 'string', 'description': 'Filter by node kind', 'enum': ['function', 'method', 'class', 'interface', 'type', 'variable', 'route', 'component']}, 'limit': {'type': 'number', 'description': 'Maximum results (default: 10)', 'default': 10}, 'projectPath': {'type': 'string', 'description': 'Path to a different project with .codegraph/ initialized. If omitted, uses current project. Use this to query other codebases.'}}, 'required': ['query']}


async def codegraph_trace(caller: McpCaller, *, from_: str, to: str, projectPath: str | None = None) -> Any:
    """Call path between two symbols — "how does <from> reach <to>?" Returns the chain with each hop's body inlined plus the destination's callees, in ONE call. Ideal for flow questions (update→render, request→handler, QuerySet→SQL). If no static path exists the chain broke at dynamic dispatch — the failure response inlines both endpoints + their TO-file siblings.

    Args:
        from_: Symbol the flow starts at (e.g., "QuerySet", "handleRequest", "mutateElement")
        to: Symbol the flow should reach (e.g., "execute_sql", "render", "setState")
        projectPath: Path to a different project with .codegraph/ initialized. If omitted, uses current project. Use this to query other codebases.
    """
    args: dict[str, Any] = {"from": from_, "to": to}
    if projectPath is not None:
        args["projectPath"] = projectPath
    return await caller.call(SERVER, "codegraph_trace", args)

codegraph_trace.__schema__ = {'type': 'object', 'properties': {'from': {'type': 'string', 'description': 'Symbol the flow starts at (e.g., "QuerySet", "handleRequest", "mutateElement")'}, 'to': {'type': 'string', 'description': 'Symbol the flow should reach (e.g., "execute_sql", "render", "setState")'}, 'projectPath': {'type': 'string', 'description': 'Path to a different project with .codegraph/ initialized. If omitted, uses current project. Use this to query other codebases.'}}, 'required': ['from', 'to']}
