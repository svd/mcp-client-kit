"""Generated MCP wrappers for the 'cloudflare-docs' server. DO NOT hand-edit signatures.

Generated by mcpgen from a live tools/list. Each function forwards to an
injected McpCaller (see mcpgen.seam); the auth/transport backend is the
caller's concern, not this module's.
"""
from __future__ import annotations

from typing import Any

from mcpgen.seam import McpCaller

SERVER = 'cloudflare-docs'



async def migrate_pages_to_workers_guide(caller: McpCaller) -> Any:
    """ALWAYS read this guide before migrating Pages projects to Workers."""
    return await caller.call(SERVER, "migrate_pages_to_workers_guide", {})

migrate_pages_to_workers_guide.__schema__ = {'type': 'object', '$schema': 'https://json-schema.org/draft/2020-12/schema', 'properties': {}}


async def search_cloudflare_documentation(caller: McpCaller, *, query: str) -> Any:
    """Search the Cloudflare documentation.

    		This tool should be used to answer any question about Cloudflare products or features, including:
    		- Workers, Pages, R2, Images, Stream, D1, Durable Objects, KV, Workflows, Hyperdrive, Queues
    		- AI Search, Workers AI, Vectorize, AI Gateway, Browser Run
    		- Zero Trust, Access, Tunnel, Gateway, Browser Isolation, WARP, DDOS, Magic Transit, Magic WAN
    		- CDN, Cache, DNS, Zaraz, Argo, Rulesets, Terraform, Account and Billing

    		Results are returned as semantically similar chunks to the query.

    Args:
        query:
    """
    args: dict[str, Any] = {"query": query}
    return await caller.call(SERVER, "search_cloudflare_documentation", args)

search_cloudflare_documentation.__schema__ = {'type': 'object', '$schema': 'https://json-schema.org/draft/2020-12/schema', 'properties': {'query': {'type': 'string'}}, 'required': ['query']}
