"""Generated MCP wrappers for the 'git' server. DO NOT hand-edit signatures.

Generated by mcpgen from a live tools/list. Each function forwards to an
injected McpCaller (see mcpgen.seam); the auth/transport backend is the
caller's concern, not this module's.
"""
from __future__ import annotations

from typing import Any

from mcpgen.seam import McpCaller

SERVER = 'git'



async def git_add(caller: McpCaller, *, repo_path: str, files: list[str]) -> Any:
    """Adds file contents to the staging area

    Args:
        repo_path:
        files:
    """
    args: dict[str, Any] = {"repo_path": repo_path, "files": files}
    return await caller.call(SERVER, "git_add", args)

git_add.__schema__ = {'properties': {'repo_path': {'title': 'Repo Path', 'type': 'string'}, 'files': {'items': {'type': 'string'}, 'title': 'Files', 'type': 'array'}}, 'required': ['repo_path', 'files'], 'title': 'GitAdd', 'type': 'object'}


async def git_branch(caller: McpCaller, *, repo_path: str, branch_type: str, contains: Any | None = None, not_contains: Any | None = None) -> Any:
    """List Git branches

    Args:
        repo_path: The path to the Git repository.
        branch_type: Whether to list local branches ('local'), remote branches ('remote') or all branches('all').
        contains: The commit sha that branch should contain. Do not pass anything to this param if no commit sha is specified. Default: None
        not_contains: The commit sha that branch should NOT contain. Do not pass anything to this param if no commit sha is specified. Default: None
    """
    args: dict[str, Any] = {"repo_path": repo_path, "branch_type": branch_type}
    if contains is not None:
        args["contains"] = contains
    if not_contains is not None:
        args["not_contains"] = not_contains
    return await caller.call(SERVER, "git_branch", args)

git_branch.__schema__ = {'properties': {'repo_path': {'description': 'The path to the Git repository.', 'title': 'Repo Path', 'type': 'string'}, 'branch_type': {'description': "Whether to list local branches ('local'), remote branches ('remote') or all branches('all').", 'title': 'Branch Type', 'type': 'string'}, 'contains': {'anyOf': [{'type': 'string'}, {'type': 'null'}], 'default': None, 'description': 'The commit sha that branch should contain. Do not pass anything to this param if no commit sha is specified', 'title': 'Contains'}, 'not_contains': {'anyOf': [{'type': 'string'}, {'type': 'null'}], 'default': None, 'description': 'The commit sha that branch should NOT contain. Do not pass anything to this param if no commit sha is specified', 'title': 'Not Contains'}}, 'required': ['repo_path', 'branch_type'], 'title': 'GitBranch', 'type': 'object'}


async def git_checkout(caller: McpCaller, *, repo_path: str, branch_name: str) -> Any:
    """Switches branches

    Args:
        repo_path:
        branch_name:
    """
    args: dict[str, Any] = {"repo_path": repo_path, "branch_name": branch_name}
    return await caller.call(SERVER, "git_checkout", args)

git_checkout.__schema__ = {'properties': {'repo_path': {'title': 'Repo Path', 'type': 'string'}, 'branch_name': {'title': 'Branch Name', 'type': 'string'}}, 'required': ['repo_path', 'branch_name'], 'title': 'GitCheckout', 'type': 'object'}


async def git_commit(caller: McpCaller, *, repo_path: str, message: str) -> Any:
    """Records changes to the repository

    Args:
        repo_path:
        message:
    """
    args: dict[str, Any] = {"repo_path": repo_path, "message": message}
    return await caller.call(SERVER, "git_commit", args)

git_commit.__schema__ = {'properties': {'repo_path': {'title': 'Repo Path', 'type': 'string'}, 'message': {'title': 'Message', 'type': 'string'}}, 'required': ['repo_path', 'message'], 'title': 'GitCommit', 'type': 'object'}


async def git_create_branch(caller: McpCaller, *, repo_path: str, branch_name: str, base_branch: Any | None = None) -> Any:
    """Creates a new branch from an optional base branch

    Args:
        repo_path:
        branch_name:
        base_branch: Default: None
    """
    args: dict[str, Any] = {"repo_path": repo_path, "branch_name": branch_name}
    if base_branch is not None:
        args["base_branch"] = base_branch
    return await caller.call(SERVER, "git_create_branch", args)

git_create_branch.__schema__ = {'properties': {'repo_path': {'title': 'Repo Path', 'type': 'string'}, 'branch_name': {'title': 'Branch Name', 'type': 'string'}, 'base_branch': {'anyOf': [{'type': 'string'}, {'type': 'null'}], 'default': None, 'title': 'Base Branch'}}, 'required': ['repo_path', 'branch_name'], 'title': 'GitCreateBranch', 'type': 'object'}


async def git_diff(caller: McpCaller, *, repo_path: str, target: str, context_lines: int | None = None) -> Any:
    """Shows differences between branches or commits

    Args:
        repo_path:
        target:
        context_lines: Default: 3
    """
    args: dict[str, Any] = {"repo_path": repo_path, "target": target}
    if context_lines is not None:
        args["context_lines"] = context_lines
    return await caller.call(SERVER, "git_diff", args)

git_diff.__schema__ = {'properties': {'repo_path': {'title': 'Repo Path', 'type': 'string'}, 'target': {'title': 'Target', 'type': 'string'}, 'context_lines': {'default': 3, 'title': 'Context Lines', 'type': 'integer'}}, 'required': ['repo_path', 'target'], 'title': 'GitDiff', 'type': 'object'}


async def git_diff_staged(caller: McpCaller, *, repo_path: str, context_lines: int | None = None) -> Any:
    """Shows changes that are staged for commit

    Args:
        repo_path:
        context_lines: Default: 3
    """
    args: dict[str, Any] = {"repo_path": repo_path}
    if context_lines is not None:
        args["context_lines"] = context_lines
    return await caller.call(SERVER, "git_diff_staged", args)

git_diff_staged.__schema__ = {'properties': {'repo_path': {'title': 'Repo Path', 'type': 'string'}, 'context_lines': {'default': 3, 'title': 'Context Lines', 'type': 'integer'}}, 'required': ['repo_path'], 'title': 'GitDiffStaged', 'type': 'object'}


async def git_diff_unstaged(caller: McpCaller, *, repo_path: str, context_lines: int | None = None) -> Any:
    """Shows changes in the working directory that are not yet staged

    Args:
        repo_path:
        context_lines: Default: 3
    """
    args: dict[str, Any] = {"repo_path": repo_path}
    if context_lines is not None:
        args["context_lines"] = context_lines
    return await caller.call(SERVER, "git_diff_unstaged", args)

git_diff_unstaged.__schema__ = {'properties': {'repo_path': {'title': 'Repo Path', 'type': 'string'}, 'context_lines': {'default': 3, 'title': 'Context Lines', 'type': 'integer'}}, 'required': ['repo_path'], 'title': 'GitDiffUnstaged', 'type': 'object'}


async def git_log(caller: McpCaller, *, repo_path: str, max_count: int | None = None, start_timestamp: Any | None = None, end_timestamp: Any | None = None) -> Any:
    """Shows the commit logs

    Args:
        repo_path:
        max_count: Default: 10
        start_timestamp: Start timestamp for filtering commits. Accepts: ISO 8601 format (e.g., '2024-01-15T14:30:25'), relative dates (e.g., '2 weeks ago', 'yesterday'), or absolute dates (e.g., '2024-01-15', 'Jan 15 2024'). Default: None
        end_timestamp: End timestamp for filtering commits. Accepts: ISO 8601 format (e.g., '2024-01-15T14:30:25'), relative dates (e.g., '2 weeks ago', 'yesterday'), or absolute dates (e.g., '2024-01-15', 'Jan 15 2024'). Default: None
    """
    args: dict[str, Any] = {"repo_path": repo_path}
    if max_count is not None:
        args["max_count"] = max_count
    if start_timestamp is not None:
        args["start_timestamp"] = start_timestamp
    if end_timestamp is not None:
        args["end_timestamp"] = end_timestamp
    return await caller.call(SERVER, "git_log", args)

git_log.__schema__ = {'properties': {'repo_path': {'title': 'Repo Path', 'type': 'string'}, 'max_count': {'default': 10, 'title': 'Max Count', 'type': 'integer'}, 'start_timestamp': {'anyOf': [{'type': 'string'}, {'type': 'null'}], 'default': None, 'description': "Start timestamp for filtering commits. Accepts: ISO 8601 format (e.g., '2024-01-15T14:30:25'), relative dates (e.g., '2 weeks ago', 'yesterday'), or absolute dates (e.g., '2024-01-15', 'Jan 15 2024')", 'title': 'Start Timestamp'}, 'end_timestamp': {'anyOf': [{'type': 'string'}, {'type': 'null'}], 'default': None, 'description': "End timestamp for filtering commits. Accepts: ISO 8601 format (e.g., '2024-01-15T14:30:25'), relative dates (e.g., '2 weeks ago', 'yesterday'), or absolute dates (e.g., '2024-01-15', 'Jan 15 2024')", 'title': 'End Timestamp'}}, 'required': ['repo_path'], 'title': 'GitLog', 'type': 'object'}


async def git_reset(caller: McpCaller, *, repo_path: str) -> Any:
    """Unstages all staged changes

    Args:
        repo_path:
    """
    args: dict[str, Any] = {"repo_path": repo_path}
    return await caller.call(SERVER, "git_reset", args)

git_reset.__schema__ = {'properties': {'repo_path': {'title': 'Repo Path', 'type': 'string'}}, 'required': ['repo_path'], 'title': 'GitReset', 'type': 'object'}


async def git_show(caller: McpCaller, *, repo_path: str, revision: str) -> Any:
    """Shows the contents of a commit

    Args:
        repo_path:
        revision:
    """
    args: dict[str, Any] = {"repo_path": repo_path, "revision": revision}
    return await caller.call(SERVER, "git_show", args)

git_show.__schema__ = {'properties': {'repo_path': {'title': 'Repo Path', 'type': 'string'}, 'revision': {'title': 'Revision', 'type': 'string'}}, 'required': ['repo_path', 'revision'], 'title': 'GitShow', 'type': 'object'}


async def git_status(caller: McpCaller, *, repo_path: str) -> Any:
    """Shows the working tree status

    Args:
        repo_path:
    """
    args: dict[str, Any] = {"repo_path": repo_path}
    return await caller.call(SERVER, "git_status", args)

git_status.__schema__ = {'properties': {'repo_path': {'title': 'Repo Path', 'type': 'string'}}, 'required': ['repo_path'], 'title': 'GitStatus', 'type': 'object'}
