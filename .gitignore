# MCP probe artifacts (may contain PII — never commit)
*.probe-raw.json
*.shapes.json.parts/

# Unscrubbed probe args — the pre-scrub twin of <server>.shapes.json. Local-only:
# carries real paths from the machine that ran the probe, so it is neither
# portable nor safe to publish. verify.py reads it opportunistically.
*.verify.json

# Raw session-analyzer output — superseded by the committed session-overview.md
session-analyzer.md

# Python
__pycache__/
*.py[cod]
*.pyo

# Virtual environments
.venv/
venv/
.env

# Build / packaging
*.egg-info/
dist/
.eggs/

# Test cache
.pytest_cache/

# Python tooling
.ruff_cache/
.mypy_cache/

# macOS
*.DS_Store

# Legacy backup folders
github.bak/
g.bak/

# Archived eval runs (eval_harness/ is the source package, not an output dir)
eval*/
!eval_harness/

# Docs scratch / fix notes
doc/FIXES*

# Project config
.mcp.json
.mcp.eval.json
.codegraph/
